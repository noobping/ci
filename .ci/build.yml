name: build
on:
  - manual
needs: check
tech: rust

steps:
  - name: build
    run: cargo build --release

  - name: package binary
    run: |
      mkdir -p dist
      install -Dm0755 target/release/ci "dist/ci-linux-$CI_ARCH"
